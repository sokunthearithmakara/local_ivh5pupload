name: CI Smoke Test

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Actions runner
        run: echo "Actions are working for local_ivh5pupload"
